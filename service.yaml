apiVersion: v1
kind: Service

metadata:
  name: kubernetes-demo-service

  labels:
    app: kubernetes-demo

spec:
  type: LoadBalancer

  selector:
    app: kubernetes-demo

  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
