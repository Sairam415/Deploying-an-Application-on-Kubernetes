apiVersion: v1
kind: Service

metadata:
  name: k8s-simple-service
  labels:
    app: k8s-simple-app

spec:
  type: LoadBalancer

  selector:
    app: k8s-simple-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
